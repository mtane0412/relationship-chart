/**
 * LayerFilterPanelコンポーネント（暫定プレースホルダー）
 *
 * Phase 3: レイヤーフィルター機能はプロパティグラフ方式移行に伴い廃止。
 * Phase 4 で EdgeFilterPanel（タグ・述語フィルター）として再実装予定。
 */

'use client';

import { memo } from 'react';

/**
 * レイヤーフィルターパネルコンポーネント（Phase 4 まで非表示）
 */
export const LayerFilterPanel = memo(() => {
  return null;
});

LayerFilterPanel.displayName = 'LayerFilterPanel';
